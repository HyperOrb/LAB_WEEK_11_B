<?xml version="1.0" encoding="utf-8"?>
<!--
  AndroidManifest.xml

  This is the manifest file for your Android application. It declares essential information
  about your app to the Android system.

  Key additions/modifications:
  - <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" android:maxSdkVersion="28" />:
    Declares the permission to write to external storage. This is necessary for older Android versions
    (API level 28 and below) when dealing with media files directly.
  - <provider ...>:
    Registers a FileProvider, which is crucial for securely sharing content URIs with camera/video capture intents.
    - `android:authorities="${applicationId}.camera"`: Defines the authority for the FileProvider.
    - `android:exported="false"`: Ensures the provider is not accessible to other apps directly, enhancing security.
    - `android:grantUriPermissions="true"`: Allows temporary access to content URIs.
    - <meta-data ...>: Links the FileProvider to the `file_provider_paths.xml` file, which specifies
      the accessible file paths.
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" android:maxSdkVersion="28" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.LAB_WEEK_11_B">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.LAB_WEEK_11_B">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.camera"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_provider_paths" />
        </provider>

    </application>

</manifest>